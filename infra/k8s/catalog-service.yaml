apiVersion: v1
kind: Service
metadata:
  name: catalog
  namespace: microservices
spec:
  selector:
    app: catalog
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
